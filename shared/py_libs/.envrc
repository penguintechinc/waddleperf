# direnv configuration for py_libs
# Auto-activates Python virtual environment

# Create and activate virtual environment
layout python3

# Set Python path to include the package
export PYTHONPATH="${PWD}:${PYTHONPATH}"

# Optional: Set development mode
export PY_ENV=development
